export type TProduct = {
  name: string;
  description: string;
  category: string;
  status: string;
  price: number;
  isDeleted: boolean;
  quantity: number;
  sellCount: number;
  image?: string;
};
